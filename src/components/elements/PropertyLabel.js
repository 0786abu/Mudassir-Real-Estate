
const PropertyLabel = ({ labels }) => {
  return (
    <>
              <div>
                <span className={`label bg-black`}>{labels}</span>
              </div>
    </>
  );
};

export default PropertyLabel;
